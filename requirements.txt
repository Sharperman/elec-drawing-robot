# ============================================================
# 电气图纸绘制机器人 - Python 依赖
# ============================================================

# Web 框架
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9

# Pydantic & 配置
pydantic>=2.7.1
pydantic-settings>=2.3.1

# LangChain 生态（langchain 1.x + langchain-classic 提供 AgentExecutor 兼容）
langchain>=1.0.0
langchain-classic>=1.0.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
openai>=1.40.0

# 向量数据库
chromadb>=0.5.3

# 数据库
sqlalchemy>=2.0.30
aiosqlite>=0.20.0

# 图像处理
pillow>=10.4.0
opencv-python-headless>=4.10.0
numpy>=1.26.0

# YOLOv8
ultralytics>=8.2.0

# AutoCAD COM (Windows only - skip on Linux CI runners)
pywin32>=306; sys_platform == 'win32'

# 日志
loguru>=0.7.2

# HTTP 客户端
httpx>=0.27.0
aiofiles>=23.2.1

# SSE
sse-starlette>=2.1.2

# 工具类
python-dotenv>=1.0.1
tenacity>=8.4.1

# 开发/调试（可选）
pytest>=8.2.2
pytest-asyncio>=0.23.7
